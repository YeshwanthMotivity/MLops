# Airflow Core
AIRFLOW_UID=50000
AIRFLOW__CORE__FERNET_KEY=  # Generate using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# CVAT Integration
CVAT_URL=http://localhost:8080
CVAT_USERNAME=admin
CVAT_PASSWORD=admin

# MLflow
MLFLOW_TRACKING_URI=http://localhost:5000

# Training & Inference
MODEL_PATH=/opt/model/best.pt
